language: python
python:
 - "2.5"
 - "2.6"
 - "2.7"

before_script:
 - python setup.py install
 - chmod u+x tests/test_shutils.py
 - chmod u+x tests/test_utils.py
 - export DV_DIR=`pwd`
 - export PYTHIA_VERSION=0

script: make -C tests -f Make.mm test
